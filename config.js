/**
 * Client-side bootstrap (served as /config.js).
 *
 * Optional: set baseUrl when REST APIs live on another origin (see index.js / room.js).
 * Example: var baseUrl = 'https://api.example.com';
 */
